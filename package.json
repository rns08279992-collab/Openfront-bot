{
  "name": "openfront",
  "private": true,
  "scripts": {
    "test": "vitest run browser/page-adapter/advisors/__tests__ browser/page-adapter/copilot/__tests__",
    "typecheck": "tsc --noEmit",
    "extract-mechanics": "node scripts/extract-mechanics.ts",
    "eval:prepare": "node scripts/prepare-local-eval.mjs",
    "eval:local": "node server/eval/run-local-eval.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
